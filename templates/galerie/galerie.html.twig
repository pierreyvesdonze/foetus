{% extends 'base.html.twig' %} {% block title %}Foetus{% endblock %} {% block
body %}
<div class="flexboxx">

  {% for image in images %}
  <div class="item">
    <img
      src="{{ asset( image.pathName ) }}"
      alt=""
      class="foetus-home-img"
    />
    {# <p class="title"></p> #}
  </div>
  {% endfor %}
</div>

{% endblock %}
