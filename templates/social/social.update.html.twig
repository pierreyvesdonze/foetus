{% extends 'base.html.twig' %} {% block title %}Modifier un lien{% endblock %} {% block
body %}

{% include 'admin/_nav.admin.html.twig' %}

<div class="main-container">
  <h1 class="big-title">Modifier un réseau social</h1>
  <div class="control-a">
    <a href="{{ path('social_show') }}">
      <span class="ti-back-left custom-button-2"></span>
    </a>
  </div>
  
  <div class="custom-flex-container">
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="form-errors">
      {{ form_errors(form) }}
    </div>
    
    {{ form_row(form.title) }}
    <hr class="hr">
    {{ form_row(form.linkPath) }}
    {{ form_row(form.iconPath) }}
    <hr class="hr">
    {{ form_row(form.save) }}
    {{ form_end(form) }}
  </div>
</div>
  
  {% endblock %}
  